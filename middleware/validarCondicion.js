// const {Item} = require ("../models/instrumentos");
// const validarCondicion = async (req, res, next) => {
//     const item = await Item.findOne(req.params.id)
//     if (item !== null) {
//         next();
//     } else {
//         res.json({msg:"El item es usado"})
//     }
// }

// module.exports = {validarCondicion}